# 28b918b 代码审查

## 范围与结论

- 对比提交 `28b918b265c956098a44c56b3a8a8fb390ce1444` 与父提交，审查同步状态刷新、拓扑缓存、清理回读和调用入口。
- 读取根目录 `AGENTS.md`；未发现相关目录的更具体指令文件。
- 本地 SDK HEAD 与 workspace 锁定版本均为 `a6246b1b0409824a3227a9c7cad8140219feb182`，核对了真实订阅访问器和组成员语义。
- 发现两个需要修复的问题：主线程中的不可分片全空间计算，以及清理复用未验证持久化 Space 字段。
- 未修改生产代码或 SDK；保留用户已有的 `SunSmart.xcscheme` 工作区改动。

## P1：首次全空间计算突破主线程分片预算

位置：`SunSmart/Common/Data/NodeSyncStatusRefresh.swift` 的节点状态计算。

刷新器在主队列执行 `getNeedSyncGroup()`。其首次邻近照明读取会进入 `NodeSyncReadContext.plan`，同步执行整个空间的拓扑构建；6 ms 检查在函数返回后才发生，无法限制这段工作。如果涉及 Site Zone，首次目标快照还可能读取其他空间。

补充复测在 `/tmp/review_28b918b_main_budget.swift`：复用提交中的测试组装器和生产刷新器、上下文及拓扑代码，设置 500 个节点、50 个邻近照明组、每节点 20 个 Model，每组 10 个节点。订阅判断恢复为锁定 SDK 的字符串集合查询和十六进制地址格式化，而非原测试替身的整数查询。

- 仅请求第一个节点的状态，返回时位于主线程。
- 首次返回耗时约 0.3265 秒。
- 排在该工作之后的另一个主队列回调同样被延迟约 0.3266 秒。

这是 macOS 隔离复测，不是原 iPad 数据或完整 SDK 集成测量；已足以展示生产调度器无法给首次计划构建让出队列。此前三个入口在后台执行这部分计算，新实现将其变成主线程停顿。应进一步拆分全空间工作，或使用隔离快照进行后台计算；修复后按 `AGENTS.md` 第 35 行要求，在真机验证最大片段和交互响应。

## P2：导出复用没有证明 Space 字段来自持久化快照

位置：`SunSmart/Common/Data/SpaceSyncCleanupCoordinator.swift` 第 186–188 行。

首轮导出来自调用者传入的 `space`。`SpaceData.export` 虽然读取持久化 Mesh 数据，但 Space 的触发区域及其解码失败标志来自该对象本身。因此数据库 revision 在读取期间不变、重新加载的 Space 时间戳相同，并不能证明首轮导出包含持久化 Space 的实际字段。项目时间戳是秒级，导入逻辑也存在同时间戳更新分支；不能以时间戳替代对象内容一致性。

如果调用者持有旧 Space 对象，而数据库中的同时间戳 Space 存在待修复或损坏的触发区域，复用会跳过对这些实际数据的导出和 normalize。后续 `extensionChanges` 不验证触发区域；流程仍可能调用 `finishSyncReferenceCleanup` 并清除保护状态。父提交通过重新加载对象导出，能够阻止这种完成。

补充复测基于原提交的持久化边界替身，保留父提交和当前提交各自的生产 `perform` 方法，让输入 Space 与数据库 Space 为不同对象、时间戳相同，只有后者的导出需要修复：

| 版本 | 清理成功 | 导出次数 | 完成清理次数 |
| --- | --- | --- | --- |
| 父提交 | false | 2 | 0 |
| 当前提交 | true | 1 | 1 |

临时复测文件：`/tmp/review_28b918b_readback_parent.swift`、`/tmp/review_28b918b_readback_head.swift`。该测试验证复用控制流，不声称替代真实数据库集成测试。应从重新加载的 Space 生成首轮可复用导出，或对比持久化 Space 的相关字段及加载状态。

## 已验证与限制

- `python3 scripts/check_node_sync_status_refresh.py`：通过。
- `python3 scripts/check_space_sync_readback_reuse.py`：通过。
- `git diff --check 28b918b^ 28b918b`：通过。
- `project.pbxproj` 可解析，五个品牌 target 中两个新增 Swift 文件均各出现一次。
- 本次未执行完整 Xcode 构建、真机 UI 验证、BLE 或云端操作，未使用 Computer Use。
- 建议人工使用实际多分组、500 节点空间检查首次进入、滚动复用、同步状态刷新期间的主线程停顿；另补真实数据库中旧 Space 对象与当前持久化字段不同的清理回读用例。
