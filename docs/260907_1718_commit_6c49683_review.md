# Commit 6c49683 审查

## 结论

发现一项 P2 回归：导入后的同步页面重新计算任务时，仅使用生命周期 preview 的候选节点，遗漏已不属于邻近照明拓扑、但仍需 Disable 的设备。

- 位置：`SunSmart/Main/Space/Controller/SpaceViewController.swift`，781–786 行。
- 场景：导入普通 Profile、无 Path/Zone 的配置，但节点保存的邻近照明状态仍为启用。导入端遍历全部节点可生成 Disable；页面 preview 仅选取当前拓扑候选节点，返回空任务并清除请求。
- 建议：保留按最新网络重新计算的方式，但与导入端一致，遍历完整目标网络的真实节点生成任务，而不是仅使用拓扑候选集合。

## 验证

- `bash scripts/check_path_topology_persistence.sh`：全部通过。
- 在临时测试编译输入中扩展现有适配层测试，使用普通 Profile、空拓扑及两个仍启用邻近照明的节点，复现导入侧生成 2 个 Disable、页面 preview 返回 0 个任务。未修改生产代码或仓库测试文件。
- 未运行 iOS 构建、手机 UI 或 BLE/Mesh 验收。
